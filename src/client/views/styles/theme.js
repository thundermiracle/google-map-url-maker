import { blue, pink } from '@material-ui/core/colors';

const theme = {
  palette: {
    primary: blue,
    secondary: pink,
  },
  typography: {
    useNextVariants: true,
  },
};

export default theme;
